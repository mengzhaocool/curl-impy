# 2026-07-13 工作日志

## Phase 2 完成
- 所有6步骤验证通过 (git: c6655c5)
- Chrome144 指纹 JA3/JA4/HTTP2 全匹配
- 6个中国网站测试通过

## Phase 3 执行
### 步骤1: win_build_full 独立构建目录 (完成)
- 复制 win_build/ + _build_all.py + patches + Chrome144.json 到 win_build_full/
- 修改 WIN_BUILD_DIR = ROOT_DIR, PATCHES_DIR = ROOT_DIR / "patches"
- 移除清理步骤中错误删除 patches/ 的代码
- 验证: 所有路径在 win_build_full/ 内，不引用 win_build/

### 步骤2: 导出依赖库API (完成)
- zstd 289个公开API函数添加到 .def 文件
- 修复 patch_cmake_dll.py: zstd 加入 /WHOLEARCHIVE 列表
- 修复: libcurl-impersonate.def 复制为 libcurl.def (CMakeLists.txt 引用)
- 移除2个 Static 符号 (ZSTD_resolveExternal*)
- x64: 289 zstd导出, 0修饰符
- x86: 289 zstd导出, 0修饰符

### 步骤3: 修复 cJSON 修饰符 (完成)
- cJSON.h CJSON_STDCALL 从 __stdcall 改为空 (cdecl)
- x86 导出从 _cJSON_xxx@N 变为 cJSON_xxx (无修饰符)

### 步骤4: stdcall 回调 (完成)
- 23个curl回调typedef改为 __stdcall
- 非静态内部回调: Curl_client_write, Curl_ftp_parselist, Curl_fnmatch, Curl_share_lock, Curl_share_unlock
- 静态mime.c回调: 创建__stdcall包装器 (mime_mem_read_cb 等5个)
- CRT函数: 创建__stdcall包装器 (curl_fread_callback, curl_fseek_callback)
- 脚本: add_stdcall_callbacks.py 自动化处理 typedef + 包装器注入
- x86+x64均编译通过，真实网站测试通过

## 导出验证 (完成)
- zstd: 289个导出 (x86+x64)
- BoringSSL: 1058个导出 (SSL_+EVP_+RSA_)
- zlib: 存在 (zlibVersion等)
- brotli: 36个导出
- x86/x64: 0个@?修饰符

## 真实网站测试 (通过)
- Chrome144: JA3/JA4指纹匹配
- Baidu/JD/Tencent: ret=0, HTTP 200

## 补充测试结果 (2026-07-13 08:00)
### 严格测试发现的Bug (2026-07-13 11:00)
- **Bug 1 MERGE函数崩溃**: 已修复 ✅ (git: b81e83c)
  - 根因3个问题:
    1. http.c缺少#include "slist.h" → Curl_slist_duplicate隐式声明 → x64指针截断
    2. 函数定义缺少static关键字 → 与前向声明不匹配 → MSVC生成错误代码
    3. 多余的int i变量
  - 修复: fix_merge_forward_decl.py添加slist.h包含+static+移除int i
- **Bug 2 Secure cookie**: 非bug — Curl_secure_context将127.0.0.1视为安全(W3C规范)
- **Bug 3 100 cookie→50**: 非bug — MAX_SET_COOKIE_AMOUNT=50是curl默认限制

### RFC 7230头大小写分析
- CURLOPT_HTTPHEADER: 非特殊头不去重，用户全权控制
- 特殊头(Host/Connection等): 大小写不敏感跳过
- 模拟merge: 大小写不敏感去重(用户覆盖base)
- 行为与系统curl一致，符合RFC 7230实践做法

### 测试通过项
- 代理独立性: 环境变量代理被忽略 ✅, IE代理被忽略 ✅, CURLOPT_PROXY生效 ✅
- R1 IP对照: DLL直连IP(119.98.94.95)==curl直连IP ✅
- R2 头大小写: DLL与curl行为一致(保留两个头,值取第一个) ✅
- R3 Cookie路径: Path=/api正确匹配 ✅, 过期cookie不发送 ✅, 多Set-Cookie全获取 ✅
- R3.2 BUG: Secure cookie在HTTP下被发送 ❌
- R4 API导出: BoringSSL 2059, nghttp2 425, zstd 289, curl 86, cJSON 78, brotli 36, zlib ~70
- R5 CURLOPT_ENCODING覆盖: ✅, HTTPHEADER覆盖: ✅, 空值移除: ✅
- R5 BUG: CURLOPT_USERAGENT+模拟崩溃 ❌
- C4 x86 stdcall: 12/12回调类型ESP_OK=1 ✅, 10次压力测试通过 ✅
- Cookie: 多Set-Cookie全获取 ✅, 跨请求持久化 ✅, COOKIEJAR/COOKIEFILE ✅, CURLOPT_COOKIE ✅
- 头大小写: DLL行为与系统curl一致（两者都保留两个头）
- 淘宝400根因: HTTP/2导致，HTTP/1.1下DLL和curl.exe都返回200
- 导出API可调用: zlibVersion/ZSTD_versionNumber/BrotliDecoderVersion/SSL_library_init ✅
- 用户覆盖: CURLOPT_ENCODING=identity成功覆盖模拟内置头 ✅
- 模拟头过滤: cookie/referer/origin/H2伪头/sec-fetch-site/dest均被过滤 ✅

## Git 基线
- f617eb6: curl.patch自包含重构 (所有merge修复合并到patch中)
- b81e83c: Bug 1修复 (merge函数崩溃: slist.h/static/int i)
- 168e685: HTTP/2修复 + stdcall fwrite包装器 + x86栈验证
- 8ba52a8: HTTP/1.1头注入 + use-after-free修复

## C1-C8复杂场景测试 (2026-07-13 13:00)
- C1.1 handle复用5URL: PASS ✅
- C1.2 Cookie跨请求: PASS ✅
- C1.3 头无残留: FAIL (curl_easy_reset不清除state.merged_headers, 设计缺陷)
- C1.4 100次循环: PASS ✅ (内存+0.03MB, 句柄不变)
- C1.5 10handle并发: PASS ✅
- C2 参数边界: 11/14 PASS (3个FAIL是Python server限制, DLL无崩溃)
- C3 错误恢复: 7/7 PASS ✅ (域名交替/超时/TLS失败/500/301/302)
- C4 补充回调: 3个注册无崩溃 (SSL_CTX/RESOLVER_START/TRAILER, triggered=0)
- C5 稳定性: 1000次循环全部成功, 内存增长4.69MB (可能DNS/SSL cache)
- C6-C8: 待执行 (TLS边界/HTTP2深度/模拟方案深度)
- 57569e4: Phase 3 完整完成 (stdcall + 导出验证)
- 1fde78d: Phase 3 部分 (win_build_full + zstd导出)
- c6655c5: Phase 2 完成
- 4867271: Phase 2.1 Chrome144验证
- a84f9b4: Phase 1 从0构建成功
