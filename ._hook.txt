【最高工作纪律，必须遵守】
1、禁止直接修改任何依赖库的源码， 任何源码修改都必须用 patches 补丁的形式进行修改，禁止触碰curl/zlib/brotli/nghttp2/zstd/BoringSSL 等依赖的源码！
2、确保编译的时候，全部删除下载的依赖源码，重新下载并应用补丁，从0构建
3、确保MIT静态链接，编译的最终产物x86/x64的dll和lib都不依赖除了Windows安装包自带的DLL以外的其他任何DLL
4、每次编译成功后必须提交git基线，保存







---------------
第一阶段：
1、清空目录，从  D:\curl-impersonate-Xweb5 复制所有文件到本目录
2、删除不需要的所有测试文件，跟构建无关的所有文件，包括依赖文件，确保从0构建可行
3、测试从0构建的可行性，如果不行，删除依赖源码，重新下载所有依赖源码，再跟 D:\curl-impersonate-Xweb5 里的依赖源码进行对比，找出差异，在本目录修改补丁应用这些差异，反复测试直到完全从0开始下载并构建成功


完成后，开始第二阶段：
1、删掉 ci前缀机制 和 curl_easy_init初始化的时候自动模拟Xweb5的逻辑，然后测试是否编译成功，测试 注册D:\curl-impersonate\Chrome144.json的模拟方案，模拟Chrome144 访问 https://120.26.33.71/json/detail，验证指纹是否一致（如果成功就提交git基线） 强调：必须编译x64/x86的DLL和lib，必须完全无外部第三方DLL依赖
2、检查注册模拟方案的时候是否注入了跟浏览器无关的请求头，比如referer，cookies 等等，确保只注入跟浏览器特征有关的头，不注入跟具体网页/网站相关的头，如果有问题就修复/编译/测试，成功后提交git基线
3、 请求头和响应头的处理是否符合HTTP官方规范，必须阅读HTTP协议规范，然后谨慎处理，特别是响应头里的set-cookie的处理，头名的大小写处理，各种头的覆盖还是合并的出来（HTTP官方规范规定了哪些头必须覆盖，如：length，keep-alive 等） 有些头必须合并，如cookies/accept/accept-encoding/accept-language/cache-control等，有些头不合并也不覆盖，同时保留，如响应头的Set-Cookie 等，这些必须深度论证，熟悉官方规范，参考curl本身的源码，尽量信任curl本身的源码，只修改你自己修改过的部分即可，如果有问题就修复/编译/测试，成功后提交git基线
4、检查现在的版本，看看是否受系统全局代理设置/IE代理设置的影响，如果是，则需要删掉这些影响，确保curl独立，用户不设置代理就直连，用户设置代理才用代理，跟环境变量和全局设置都无关，如果有问题就修复/编译/测试，成功后提交git基线
5、最后再检查模拟Chrome144 访问 https://120.26.33.71/json/detail，验证指纹是否一致（如果成功就提交git基线）
6、访问更多中国的网站，如淘宝，天猫，百度，京东，阿里，腾讯等网站首页，确认curl没有任何问题，cookies相关的函数完全没有任何问题


 强调：必须编译x64/x86的DLL和lib，必须完全无外部第三方DLL依赖
全部没有问题后就提交基线，删除所有测试文件，和依赖，保持项目目录结构干净




然后，第三阶段：
1、创建一个新目录：win_build_full 目录，先复制win_build里面的所有文件，里面的文件架构跟 win_build 完全相同，修改里面各个文件里的路径定义，改为相对路径，确保依赖的下载，编译的流程都在win_build_full 目录内，跟原来的win_build完全不相干
2、然后在 win_build_full 目录 添加补丁：
3、把 zlib/brotli/zstd/BoringSSL 的公开函数API，全部从DLL里导出，且验证，确保x86/x64的DLL的导出函数里没有@?等乱七八糟的修饰符符号
4、把curl，BoringSSL ，brotli，zstd，zlib 里面所有需要回调的函数，回调函数的调用约定改为 stdcall‌ 约定，并仔细论证和测试，主要是对x86的影响（API函数不需要改，保持原有即可，只改回调函数）
这些反复修改测试，确定没有问题后，访问上述真实网站进行测试，确定没有问题后提交git基线
强调：必须确保不影响win_build目录，不读取修改win_build目录的文件，确保从0构建，


